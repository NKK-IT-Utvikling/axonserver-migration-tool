name: CodeQL

on:
  push:
    branches: [main, master]
  pull_request:
  schedule:
    - cron: '0 14 * * 1'

jobs:
  analyze:
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: NKK-IT-Utvikling/nkk-actions/.github/workflows/codeql.yml@main
    secrets: inherit
