package io.axoniq.axonserver.migration;

// Do not change the order of the enum values, ordinal is used as id in migration status
public enum MigrationContext {
    DEFAULT,
    KULL,
    TERMINLISTE
}
